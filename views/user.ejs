<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title%>
    </title>
</head>
<body>
    <%- include('_header'); %>
    <h1>welcome to the users-profile</h1>
    

    <% if(user.id==profile_user.id){%>
<!-- if user matches then show the form else show only the profile info-->
        <form action="/user/update/<%= profile_user.id%>" method="post">
            <!-- user has email password and name -->
            <!-- we are providing to update name and email -->
            <input type="text" name="name" placeholder="Your name" value="<%= profile_user.name%>" required>
            <input type="email" name="email" placeholder="Your email" value="<%= profile_user.email%>" required>
            <input type="submit" value="Update">
        </form>
<%} else {%>
    <p><%= profile_user.name%></p>
        <p><%= profile_user.createdAt%></p>
        <p><%= profile_user.email%></p>
    <%}%>
    <%- include('_footer'); %>
</body>
</html>